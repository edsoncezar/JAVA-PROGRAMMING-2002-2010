<APPLET CODE="MouseMotionEventDemo.class"
        ARCHIVE="example-swing/mousemotion.jar"
        WIDTH="450" HEIGHT="200"
        ALT="Your browser understands the applet tag but isn't displaying any applet.">

Since you can't run the applet,
here's a picture of it:

#usemacro UI_FIGURE (
    NAME MouseMotionEventDemo.gif
    W 450
    H 200
    ALT A snapshot of MouseMotionEventDemo, which demonstrates events that are fired when the user moves the mouse.
)

</APPLET>

