<APPLET CODE="WindowEventDemo.class"
        ARCHIVE="example-swing/window.jar"
        WIDTH="400" HEIGHT="200"
        ALT="Your browser understands the applet tag but isn't displaying any applet.">

Since you can't run the applet,
here's a picture of it:

#usemacro UI_FIGURE (
    NAME WindowEventDemo.gif
    W 400
    H 200
    ALT A snapshot of WindowEventDemo, which demonstrates the events that are fired when the window is resized, hidden, and so on. 

)

</APPLET>

