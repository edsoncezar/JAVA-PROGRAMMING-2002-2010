<?xml version="1.0"?>

<!-- ======================================================================= -->
<!-- JBoss build file                                                       -->
<!-- ======================================================================= -->

<project name="JBoss" default="ejbjar" basedir=".">

    <property environment="env"/>
    <property name="src.dir" value="${basedir}/src/main"/>
    <property name="src.resources" value="${basedir}/src/resources"/>
    <property name="jboss.home" value="${env.JBOSS_HOME}"/>
    <property name="build.dir" value="${basedir}/build"/>
    <property name="build.classes.dir" value="${build.dir}/classes"/>
    <property name="build.compiler" value="classic"/>

  <!-- Build classpath -->
  <path id="classpath">
        <fileset dir="${jboss.home}/client">
            <include name="**/*.jar"/>
        </fileset>
	<pathelement location="${build.classes.dir}"/>
	<!-- So that we can get jndi.properties for InitialContext -->
	<pathelement location="${basedir}/jndi"/>
  </path>

  <!-- =================================================================== -->
  <!-- Prepares the build directory                                        -->
  <!-- =================================================================== -->
  <target name="prepare">
    <mkdir dir="${build.dir}"/>
    <mkdir dir="${build.classes.dir}"/>
  </target>

  <!-- =================================================================== -->
  <!-- Compiles the source code                                            -->
  <!-- =================================================================== -->
  <target name="compile" depends="prepare">
    <javac srcdir="${src.dir}"
           destdir="${build.classes.dir}"
           debug="on"
           deprecation="on"
           optimize="off"
           includes="**">
            <classpath refid="classpath"/>
    </javac>
  </target>

  <target name="ejbjar" depends="compile">
    <jar jarfile="build/titan.jar">
      <fileset dir="${build.classes.dir}">
      	  <include name="com/titan/**/*.class"/>
      	  <exclude name="com/titan/clients/*.class"/>
      </fileset>
      <fileset dir="${src.resources}/beans/">
          <include name="**/*.xml"/>
      </fileset>
     </jar>
     <copy file="build/titan.jar" todir="${jboss.home}/server/default/deploy"/>
  </target>

  <!-- =================================================================== -->
  <!-- Runs the tests                                                      -->
  <!-- =================================================================== -->

  <target name="make-topic">
     <copy file="${src.resources}/services/jbossmq-titantopic-service.xml" todir="${jboss.home}/server/default/deploy"/>
  </target>

  <target name="createdb_121" depends="compile">
    <java classname="com.titan.clients.Client_121b" fork="yes" dir=".">
      <classpath refid="classpath"/>
      <arg line="CreateDB"/>
    </java>
  </target>

  <target name="dropdb_121" depends="compile">
    <java classname="com.titan.clients.Client_121b" fork="yes" dir=".">
      <classpath refid="classpath"/>
      <arg line="DropDB"/>
    </java>
  </target>

  <target name="run.client_122a" depends="ejbjar">
    <java classname="com.titan.clients.Client_122a" fork="yes" dir=".">
      <classpath refid="classpath"/>
    </java>
  </target>

  <target name="run.client_122b" depends="ejbjar">
    <java classname="com.titan.clients.Client_122b" fork="yes" dir=".">
      <classpath refid="classpath"/>
      <arg line="${param.cust} ${param.cruise} ${param.cabin} ${param.price}"/>
    </java>
  </target>

  <target name="run.client_122c" depends="ejbjar">
    <java classname="com.titan.clients.Client_122c" fork="yes" dir=".">
      <classpath refid="classpath"/>
      <arg line="${param.cruise} ${param.bedcount}"/>
    </java>
  </target>

  <target name="run.client_131" depends="ejbjar">
    <java classname="com.titan.clients.JmsClient_1" fork="yes" dir=".">
      <classpath refid="classpath"/>
    </java>
  </target>

  <!-- =================================================================== -->
  <!-- Cleans up generated stuff                                           -->
  <!-- =================================================================== -->
  <target name="clean.db">
    <delete dir="${jboss.home}/server/default/db/hypersonic"/>
  </target>
  <target name="clean">
    <delete dir="${build.dir}"/>
    <delete file="${jboss.home}/deploy/titan.jar"/>
    <delete file="${jboss.home}/server/default/deploy/jbossmq-titantopic-service.xml"/>
  </target>

</project>

