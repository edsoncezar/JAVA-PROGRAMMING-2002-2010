// chamada de funcao do oracle em java 
CallableStatement cstmt = new con.prepareCall("{?=call SUA_FUNCAO(?)}");
// Voce seta o valor do parametro = dep.O 2 é a "?". 
cstmt.setInt(2, dep); 
// Depois vc precisa esperar o retorno da sua function. 
cstmt.registerOutParameter(1, java.sql.Types.INTEGER); 
cstmt.execute(); 
int x = cstmt.getInt(1);// O 1 é a primeira "?". 
